from operators import calculate_times
from src.algorithms.interface import AlgorithmOutput
from typing import List

def rank_selection(population: List[AlgorithmOutput], costs: List[int]) -> List[AlgorithmOutput]:
